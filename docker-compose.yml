services:
    backend:
        build: Back-end
        dockerfile: Dockerfile
        container_name: backend
        ports:
            - "9091:9091"
        restart: always
        networks:
    frontend:
        build: Front-end
        dockerfile: Dockerfile
        container_name: frontend
        ports:
            - 9090:9090
        restart: always
        depends_on:
            - backend